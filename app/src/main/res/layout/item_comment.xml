<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/comment_item_profile_image"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginLeft="8dp"
        android:layout_marginTop="8dp"
        android:src="@drawable/like"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/comment_item_user_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="8dp"
        android:textStyle="bold"
        android:textColor="#000000"
        app:layout_constraintLeft_toRightOf="@id/comment_item_profile_image"
        app:layout_constraintTop_toTopOf="@+id/comment_item_profile_image"
        tools:text="username" />

    <TextView
        android:id="@+id/comment_item_comment"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toRightOf="@id/comment_item_profile_image"
        app:layout_constraintTop_toBottomOf="@+id/comment_item_user_name"
        android:layout_marginLeft="8dp"
        android:textColor="#000000"
        tools:text="comment" />

</androidx.constraintlayout.widget.ConstraintLayout>